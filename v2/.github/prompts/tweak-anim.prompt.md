---
mode: edit
description: 微调动画参数（时长 / 缓动 / 位移）
---

我要调整一个动画。请先问我：

1. 调哪个？（hover 浮动 / 拖拽缩放 / 释放飞出 / 其他）
2. 改什么？（时长 duration / 缓动 easing / 幅度 amplitude / 关键帧位置）
3. 目标值是多少？

确认后**只改 `styles.css`**，不要动 `index.html` 的 JS 逻辑。
改完用一句话告诉我改了哪个选择器的哪个属性。
